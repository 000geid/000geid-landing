# Optional: pin canonical/sitemap/social URLs to your production origin (no trailing slash).
# If unset, the incoming request URL origin is used (works for single-domain deploys).
# PUBLIC_SITE_URL=https://example.com
